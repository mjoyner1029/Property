# ---- Core Flask ----
Flask==3.1.2
Werkzeug==3.1.3
python-dotenv==1.1.1
gunicorn==23.0.0

# ---- Database ----
Flask-SQLAlchemy==3.1.1
SQLAlchemy>=2.0.43,<3.0
Flask-Migrate==4.1.0
alembic==1.16.5
# Python 3.13: use psycopg v3, not psycopg2
psycopg[binary]>=3.1.19

# ---- Auth / Security / CORS / Email ----
Flask-JWT-Extended==4.7.1
Flask-Talisman==1.1.0
Flask-CORS==6.0.1
bcrypt==4.3.0
email-validator==2.3.0
Flask-Mail==0.10.0

# ---- Realtime (threading backend by default; no eventlet needed) ----
Flask-SocketIO==5.5.1
# If you later switch to eventlet/gevent, add the appropriate worker here.

# ---- Payments ----
stripe==12.5.0

# ---- Observability / misc ----
prometheus-client==0.22.1
psutil==7.0.0
redis==6.4.0
Flask-Limiter==3.12
sentry-sdk==2.35.1
python-dateutil>=2.9.0

# ---- MFA / QR Code ----
qrcode>=7.4
pyotp>=2.9.0
