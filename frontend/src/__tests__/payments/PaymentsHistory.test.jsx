import React from 'react';
import PaymentsHistory from '../../pages/payments/PaymentsHistory';

describe('PaymentsHistory', () => {
  test('is defined', () => {
    expect(PaymentsHistory).toBeDefined();
  });
});
